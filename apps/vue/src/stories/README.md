# Vue Storybook Stories

These stories are scoped to the Vue adapter app. They mirror the HTML
fragment variants in `apps/html/stories` so we can validate Vue bindings with
the same visual baselines.
